/* import * as React from 'react';
import './style.css';

export default function App() {
  let [count, setCount] = React.useState(0);
  let [fecha, setFecha] = React.useState('');

  const click = () => {
    setCount(count + 1);
    setFecha(new Date().toLocaleString());
  };

  return (
    <div>
      <h1>Click Tracker</h1>
      <div>{count} </div>
      <span>{fecha}</span>
      <button onClick={click}> Click me</button>
    </div>
  );
} */
